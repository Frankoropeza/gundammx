---
import BaseLayout from '@layouts/BaseLayout.astro';
import Breadcrumbs from '@components/Breadcrumbs.astro';
import { getCollection } from 'astro:content';
import { schemaListado, schemaMigaDePan } from '@lib/seo';

const guias = (await getCollection('guides', ({ data }) => !data.borrador))
  .sort((a, b) => a.data.orden - b.data.orden);

const base = { site: Astro.site, url: Astro.url.pathname };
const migas = [{ nombre: 'Guías', href: '/guias/' }];
const schemas = [
  schemaMigaDePan(base, migas),
  schemaListado(base, 'Guías de Gunpla para México',
    guias.map((g) => ({ nombre: g.data.titulo, href: `/guias/${g.id}/` }))),
];
import HubImagen from '@components/HubImagen.astro';
import imgGuias from '../../assets/hub-guias.jpg';
---

<BaseLayout
  titulo="Guías de Gunpla | Qué comprar y cómo empezar"
  descripcion="Guías de Gunpla para México: qué kit de Gundam comprar primero, cómo distinguir un original de un bootleg y cuánto debería costar en pesos."
  schemas={schemas}
>
  <Breadcrumbs items={migas} />

  <HubImagen src={imgGuias} id="hub-guias" />

  <div class="grid-page py-8">
    <h1 class="display text-3xl md:text-4xl">Guías</h1>
    <p class="mt-4 max-w-3xl text-ink-2">
      Escritas para quien compra en México: precios en pesos, tiendas nacionales y los problemas
      reales de este mercado, que no son los mismos que en Japón o Estados Unidos.
    </p>

    <ul class="mt-8 grid gap-4 sm:grid-cols-2">
      {guias.map((g) => (
        <li class="card p-5">
          <span class="tag capitalize">{g.data.nivel}</span>
          <h2 class="mt-3 display text-lg">
            <a href={`/guias/${g.id}/`} class="hover:text-accent">{g.data.titulo}</a>
          </h2>
          <p class="mt-2 text-sm text-ink-2">{g.data.descripcion}</p>
        </li>
      ))}
    </ul>
  </div>
</BaseLayout>
